module TribeControl

# Bumped by hand.  The version lives here alone: the gemspec reads it
# from this file, and `tribe-control --version` prints the same
# constant, so a release cannot have two numbers.
VERSION = '0.1.0'.freeze

end
